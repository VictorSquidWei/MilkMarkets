{
  "name": "milk-market-functions",
  "description": "Milk Market — the single Cloud Function: a server-side Riot API proxy.",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.0.1"
  }
}
